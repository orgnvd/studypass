{
  "name": "studypass-angular",
  "version": "1.0.0",
  "description": "studypass frontend",
  "devDependencies": {
    "bower": "^1.8.8",
    "gulp": "^4.0.2",
    "gulp-replace-task": "^0.1.0",
    "gulp-sass": "~3.0.0",
    "http-proxy": "1.18.1",
    "http-server": "^0.12.0"
  },
  "scripts": {
    "postinstall": "bower install && gulp populateAppConfig",
    "prestart": "npm install",
    "start": "gulp proxy-server"
  }
}
